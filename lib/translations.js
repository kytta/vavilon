export function useTranslation(language) {
    
}

/**
 * Apply translation to the page.
 * 
 * @param {import("./language").Language} language - language to translate the page to.
 */
export function apply(language) {

}
